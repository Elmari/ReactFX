subprojects {
    version = '2.0-SNAPSHOT'

    apply plugin: 'java'
    apply plugin: 'eclipse'

    repositories {
        mavenCentral()
    }

    sourceCompatibility = '11'
    targetCompatibility = '11'
}